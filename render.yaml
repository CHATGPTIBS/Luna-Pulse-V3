services:
  - type: web
    name: luna-terminal
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/session
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: WEB_ADMIN_TOKEN
        sync: false
